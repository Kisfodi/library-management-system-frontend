import { AuthorButtonDirective } from './author-button.directive';

describe('AuthorButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new AuthorButtonDirective();
    expect(directive).toBeTruthy();
  });
});
